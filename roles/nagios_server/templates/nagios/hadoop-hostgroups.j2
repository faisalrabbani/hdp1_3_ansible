define hostgroup {
	hostgroup_name all-servers
	alias All Servers
	members changemeAll 
}

define hostgroup {
        hostgroup_name namenode
        alias namenode_{{clusterNameNagios}}
        members {{hostvars[groups['namenode'][0]]['ansible_'+interfaceNagios]['ipv4']['address'] }} 
	
}

define hostgroup {
        hostgroup_name snamenode
        alias snamenode_{{clusterNameNagios}}
        members {{hostvars[groups['secondary_namenode'][0]]['ansible_'+interfaceNagios]['ipv4']['address'] }} 
}

define hostgroup {
        hostgroup_name jobtracker
        alias jobtracker_{{clusterNameNagios}}
        members {{hostvars[groups['jobtracker'][0]]['ansible_'+interfaceNagios]['ipv4']['address'] }} 
}

define hostgroup {
        hostgroup_name slaves
        alias slaves_{{clusterNameNagios}}
	members changeme

}

define hostgroup {
        hostgroup_name nagios-server
        alias nagios-server
        members {{hostvars[groups['nagios_master'][0]]['ansible_'+interfaceNagios]['ipv4']['address'] }}
}

define hostgroup {
        hostgroup_name ganglia-server
        alias ganglia-server_{{clusterNameNagios}}
        members {{hostvars[groups['ganglia_master'][0]]['ansible_'+interfaceNagios]['ipv4']['address'] }} 
}

define hostgroup {
        hostgroup_name hiveserver
        alias hiveserver_{{clusterNameNagios}}
        members {{hostvars[groups['hiveMetastore'][0]]['ansible_'+interfaceNagios]['ipv4']['address'] }}
}

